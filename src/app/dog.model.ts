export class Dog {
  constructor(public name: string, public breed: string, public age: number, public location: string, public bio: string, public imageURL: string, public rank: string, public joined: string) {}
}
